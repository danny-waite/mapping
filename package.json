{
  "name": "mapping",
  "version": "1.0.0",
  "description": "",
  "main": "build-map.js",
  "scripts": {
    "start": "browser-sync start --server --files '*/*','*'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "rebuild": "./rebuild.sh",
    "watch": "npm-watch"
  },
  "watch": {
    "rebuild": {
      "patterns": [
        "inputs"
      ],
      "extensions": "txt"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-watch": "^0.6.0"
  }
}
